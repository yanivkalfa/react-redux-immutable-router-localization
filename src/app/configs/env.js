/**
 * you can define site config here like api address and google map keys etc.
 * @type {boolean}
 */
/*
const prod = window.NODE_ENV === 'production';
export default {
  domain: 'domain',
  socketUrl: '',
  appVersion: window.appVersion
};
*/